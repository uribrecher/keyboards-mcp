node_modules/
dist/
test_data/
data/
.DS_Store
.idea
.claude/settings.local.json
.claude/scheduled_tasks.lock
.mcp.json
coverage.lcov
coverage/
.coverage-out/

# Vendor JS bundles copied from node_modules at install/build time.
# Regenerated by `npm run copy:peaks-vendor` (wired via postinstall + prebuild),
# so they don't belong in source.
src/sounds-and-recreation-app/shell/vendor/
dist-app/
