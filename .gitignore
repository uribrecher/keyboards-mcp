node_modules/
dist/
test_data/
data/
.DS_Store
.idea
.claude/settings.local.json
.claude/scheduled_tasks.lock
.mcp.json
coverage.lcov
coverage/
.coverage-out/

# Vendor JS bundles copied from node_modules at app build time.
# Regenerated by the app's `copy:peaks-vendor` (run via its prebuild / presar* hooks),
# so they don't belong in source.
**/shell/vendor/
dist-app/
